# Notes

- 用户目标：系统化补齐部署应用时的网络处理手段，不局限于正向代理、反向代理、内网穿透、SSH Tunnel、SOCKS5。
- 用户偏好：偏工程实践，希望看到真实架构、命令、配置、排障路径。
- 第一阶段能力目标：选型判断、动手配置、安全边界、排障能力都要覆盖；每节课仍保持短小，避免一次塞太多。
- 教学语言：中文，术语可保留英文并解释。
- 输出节奏：用户希望一次生成多个课程文件，而不是每次只输出一节。
